version: '3.8'

services:
  aorata:
    build: .
    ports:
      - "3000:3000"
      - "25565-25575:25565-25575"
    volumes:
      - ./servers:/app/servers
    environment:
      - NODE_ENV=production
      - PORT=3000
